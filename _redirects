# Pretty URLs that don't share a name with an .html file get
# explicit redirects. The .html-named pages (/services, /about,
# /careers, /contact, /privacy, /terms) are handled automatically
# by Workers Static Assets via html_handling — adding _redirects
# rules for them creates a loop with the .html→pretty redirect.

/client        /client-login      301
/employee      /employee-login    301
